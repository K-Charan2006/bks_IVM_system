export enum Screen {
  LAUNCH,
  LANGUAGE,
  PHONE_ENTRY,
  ROLE_SELECTION,
  FARMER_MENU,
  MY_CROPS,
  WORKER_MENU,
  MACHINE_OWNER_MENU,
}

export type Language = 'en' | 'hi' | 'pa' | 'ta' | 'te' | 'kn' | 'ml' | 'bn';
